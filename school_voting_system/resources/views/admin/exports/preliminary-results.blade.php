<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Preliminary Results — {{ $election?->title ?? 'Assigned Election' }}</title>
    <style>
        body { font-family: system-ui, sans-serif; margin: 2rem; color: #1e293b; }
        .watermark { color: #b45309; font-weight: 700; border: 2px dashed #f59e0b; padding: 1rem; margin-bottom: 1.5rem; text-align: center; }
        footer { margin-top: 3rem; padding-top: 1rem; border-top: 1px solid #cbd5e1; color: #64748b; font-size: 0.875rem; }
        table { width: 100%; border-collapse: collapse; margin-top: 1rem; }
        th, td { border: 1px solid #e2e8f0; padding: 0.5rem 0.75rem; text-align: left; }
        th { background: #f1f5f9; }
    </style>
</head>
<body>
    <div class="watermark">⚠️ UNOFFICIAL — FOR REVIEW ONLY — NOT A FINAL LOCKED RESULT</div>
    <h1>Preliminary Results</h1>
    <p><strong>Election:</strong> {{ $election?->title ?? 'Unassigned' }}</p>
    <p><strong>Generated:</strong> {{ $generatedAt }}</p>
    <table>
        <tr><th>Metric</th><th>Value (Assigned Scope)</th></tr>
        <tr><td>Eligible Voters</td><td>{{ $stats['eligible_voters'] }}</td></tr>
        <tr><td>Votes Cast</td><td>{{ $stats['votes_cast'] }}</td></tr>
        <tr><td>Turnout</td><td>{{ $stats['turnout_percent'] }}%</td></tr>
        <tr><td>Partylists</td><td>{{ $stats['partylists'] }}</td></tr>
        <tr><td>Candidates</td><td>{{ $stats['candidates'] }}</td></tr>
    </table>
    <footer>Generated by {{ $role }} — Not an Official Final Result · {{ $generatedAt }}</footer>
</body>
</html>
